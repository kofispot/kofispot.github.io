function HeaderDetails(title, image, link, isFeatured) {
  this.title = title
  this.image = image
  this.link = link
  this.featured = isFeatured ? isFeatured : false
}

/*
By default, the header would always contain the welcome image
This function takes header details class object to create more headers
* */
function header() {
  //first we create the welcome header
}
